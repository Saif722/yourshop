.row.signup_block.text-center
  %h1 Sign up
  .container
    .row
      = form_for(@user) do |f|
        / / .col-md-12
        / -if 0errors
        /   %h2 You're already signed with this email.
        %h2= flash[:new_solution_errors]
        = react_component("SignUpForm")
        /   %span.error hello
        / .col-md-12
        /   = react_component("InputFields", {place_holder: "Enter your password",field: "password", name: "user[password]"})
        / .col-md-12
        /   = react_component("InputFields", {place_holder: "Re-Enter your password to confirm",field: "password", name: "user[password_confirmation]"})
        / .col-md-12
        /   / = f.submit "Create my account", class: "btn btn-primary"
        /   = react_component("SubmitButton", {value: "Create my account",type: "submit", disable_with: "Creating"})
